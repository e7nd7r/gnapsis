# Gnapsis Configuration Example
#
# Gnapsis uses a layered configuration approach:
# 1. Global config: ~/.config/gnapsis/config.toml (database, embedding settings)
# 2. Project config: .gnapsis.toml (project name, graph)
# 3. Environment variables: GNAPSIS_* (highest priority)
#
# This file is an example PROJECT config. Copy to .gnapsis.toml in your project root.

[project]
# Project name (required). Graph name is always gnapsis_<name>.
name = "my-project"


# ==============================================================================
# SOURCES
# ==============================================================================
# Sources define directories containing project files. Each source has an ID
# (used by references) and a path. If no sources are configured, the current
# directory is used as a "default" source.
#
# Rules:
# - 0 sources: cwd is the implicit "default" source
# - 1 source: implicitly the default regardless of its id
# - 2+ sources: one must have id = "default"
#
# [[project.sources]]
# id = "default"
# path = "/path/to/code-repo"
#
# [[project.sources]]
# id = "docs"
# path = "/path/to/documentation-vault"


# ==============================================================================
# GLOBAL CONFIG EXAMPLE (~/.config/gnapsis/config.toml)
# ==============================================================================
# The following settings typically go in your global config file, not in
# project configs. This keeps credentials out of your repositories.
#
# [postgres]
# uri = "postgresql://postgres:password@localhost:5432/gnapsis"
#
# [embedding]
# provider = "fastembed"
# model = "BAAI/bge-small-en-v1.5"
# dimensions = 384
